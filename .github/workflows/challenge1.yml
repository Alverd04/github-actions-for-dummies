name: Challenge1
on:
  pull_request:
    branches: [master]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Say Hello
        run: echo "¡Hola Caracola! 🐌"
